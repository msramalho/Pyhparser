from setuptools import setup

setup(name='masterParserPython',
      version='0.1',
      description='Parse input files (inputFile.txt) into python variables by specifying the layout of the variables (parserLayout.txt)',
      url='https://github.com/msramalho/masterParserPython',
      author='msramalho',
      license='MIT',
      packages=['masterParserPython'],
      zip_safe=False)